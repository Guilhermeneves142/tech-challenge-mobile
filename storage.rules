rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Recibos: só o dono (mesmo uid na pasta) pode ler/enviar/apagar.
    match /receipts/{userId}/{fileName} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
